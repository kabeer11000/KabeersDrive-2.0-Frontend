import React from 'react';
import './SharedView.css';

const SharedView = () => (
    <div className="SharedView">
        SharedView Component
    </div>
);

SharedView.propTypes = {};

SharedView.defaultProps = {};

export default SharedView;
